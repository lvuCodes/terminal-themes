// Terminal Themes. Copyright (C) 2026 lvuCodes. Licensed under GPL-3.0-or-later; see LICENSE.
//
// Back-link feature. A pill anchored to the page's top-left corner that leaves
// the site for the author's home page. Self-contained: the shell renders it with
// no props, and both the target and the label carry defaults, exported so
// consumer tests can assert against them instead of restating the literals.
export { BackLink, BACK_LINK_DEFAULT_HREF, BACK_LINK_DEFAULT_LABEL } from "./BackLink";
